@model IEnumerable<ASPNETINCLASSWEEK1.Models.Manufactruer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Make)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Make)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Manufactruer_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Manufactruer_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Manufactruer_ID })
        </td>
    </tr>
}

</table>
