@model IEnumerable<ASPNETINCLASSWEEK1.Models.Car>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxSpeed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Model)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaxSpeed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Car_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Car_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Car_ID })
        </td>
    </tr>
}

</table>
